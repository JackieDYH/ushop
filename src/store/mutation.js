// 自定义方法
export default {
    // 修改状态方法
    setUserinfo(state,info){
        state.userinfo = info;
    },
    setCartlist(state,info){
        state.cartlist = info;
    },
    setUrlpath(state,info){
        state.urlpath = info;
    }
}